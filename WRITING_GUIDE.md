# 博客写作教程

这份指南只讲一件事：怎样把一篇文章写进这个博客并发布出来。你不需要修改 React 或 TypeScript；日常写作只会用到 `content/posts` 目录中的 Markdown 文件。

## 先理解：一篇文章如何出现

每个 `content/posts` 下的 `.md` 文件都会自动成为一篇文章：

```text
content/posts/my-first-post.md  →  /posts/my-first-post
```

首页会自动收集这些文件，并按文章的 `date` 从新到旧排列。因此，新写文章时不需要改首页、导航或任何代码。

文件名也是文章链接的一部分。建议使用小写英文、数字和连字符，例如 `my-terminal-notes.md`；不要使用空格、中文或重复的文件名。文章发布后不要轻易改名，否则原来的链接会失效。

## 五分钟写出第一篇文章

1. 打开 `content/posts` 目录。
2. 新建一个文件，例如 `my-first-post.md`。
3. 复制下面的模板，替换其中的内容。
4. 保存文件，启动本地预览确认效果。

```md
---
title: 我的第一篇文章
excerpt: 用一两句话告诉读者这篇文章解决什么问题，或能带走什么。
date: 2026-09-03
readingTime: 4 min read
tags: [随笔, Markdown]
---

这是一段开场。先说清楚这篇文章要讨论什么，以及读者为什么值得继续读下去。

## 我想记录的问题

用一两个段落交代背景、限制条件或你原本的想法。段落不宜太长；一个段落只表达一个意思会更容易阅读。

## 我的做法

按步骤解释你的思路。需要时放入代码、图片、链接或清单。

## 最后的收获

简短总结：什么方法有效、什么仍待验证，以及读者可以从哪里开始尝试。
```

保存后，文章会出现在首页的文章列表中，地址为 `/posts/my-first-post`。

## 文章开头的资料卡（frontmatter）

文件最开始的两行 `---` 之间是文章资料卡。它必须放在文件的最顶部，字段每行写一个：

```yaml
---
title: 标题
excerpt: 摘要
date: 2026-09-03
readingTime: 4 min read
tags: [分类, 主题]
---
```

| 字段 | 怎么写 | 显示位置 |
| --- | --- | --- |
| `title` | 一句明确、具体的标题 | 文章页标题、浏览器标题、首页列表 |
| `excerpt` | 约 30～60 字的摘要 | 文章页导语、首页列表、分享描述 |
| `date` | `YYYY-MM-DD`，如 `2026-09-03` | 首页排序、文章日期 |
| `readingTime` | 如 `4 min read` | 文章标题下方 |
| `tags` | 方括号中的标签，如 `[React, 性能]` | 第一个标签显示在文章标题上方 |

这五个字段都应填写。尤其不要省略 `tags`：当前文章页会读取第一个标签作为分类标题。日期必须补齐月份和日期的两位数字，例如写 `2026-09-03`，不要写成 `2026-9-3`。

标题和摘要里可以出现中文标点。若内容本身含有引号，也可以用英文双引号把整个值包起来，例如 `title: "一份可复用的写作清单"`。

## 推荐的写作结构

好的博客文章不一定长，但应当让读者很快知道：它在解决什么、依据是什么、最后得到什么。下面的结构适合技术文章、复盘和学习笔记：

```md
开场：问题、结论或读者能获得什么。

## 背景
发生了什么？为什么这个问题值得处理？

## 过程
按时间或步骤说明尝试过什么。给出关键选择的理由。

## 结果
展示数据、代码、截图或可观察到的变化。

## 复盘
哪些做法有效？有哪些限制？下次会怎样做？
```

写作时可以把一级信息放在 `##` 标题下，把补充信息放在 `###` 标题下。不要为了调大字体而跳过标题层级；标题本身就是文章的阅读地图。

## Markdown 常用写法

### 强调、链接与引用

```md
**重要内容**、*轻微强调*、~~已废弃的说法~~。

[给读者的链接](https://example.com)

> 这是一段引用。可以用来摘录资料、强调结论或标出旁注。
```

链接文字要说明去向。比起“[点击这里](https://example.com)”，更推荐“[React 官方文档](https://react.dev/)”。

### 列表

```md
- 无序事项
- 另一个事项
  - 可以用两个空格缩进写子项

1. 有顺序的第一步
2. 有顺序的第二步

- [ ] 待完成的检查项
- [x] 已完成的检查项
```

当顺序会影响操作时用数字列表；单纯罗列要点时用无序列表。清单很适合放在文章末尾。

### 代码

在一小段文字中提到命令或变量名时，用反引号包起来：`npm run build`。

多行代码要使用三个反引号，并在开头写语言名称以获得语法高亮：

````md
```tsx
export function Greeting({ name }: { name: string }) {
  return <p>Hello, {name}.</p>;
}
```
````

常见语言名包括 `ts`、`tsx`、`js`、`jsx`、`css`、`html`、`bash`、`json` 和 `md`。代码块前后各留一个空行，且末尾的三个反引号必须和开头对应。

### 表格

```md
| 方案 | 优点 | 代价 |
| --- | --- | --- |
| 方案 A | 上手快 | 扩展性一般 |
| 方案 B | 更灵活 | 需要更多配置 |
```

表格适合横向比较两到四个维度；内容太长时，拆成小标题和列表通常会更易读。

### 数学公式

行内公式放在单个美元符号之间：

```md
欧拉恒等式是 $e^{i\pi} + 1 = 0$。
```

独立公式使用一对双美元符号：

```md
$$
\int_{-\infty}^{+\infty} e^{-x^2} \, dx = \sqrt{\pi}
$$
```

公式由 KaTeX 渲染。反斜杠是公式的一部分，应原样写入 Markdown；不用改成 JavaScript 字符串的双反斜杠。

## 给文章添加图片

1. 将图片放到 `public/images`。目录不存在时可以新建它。
2. 使用小写英文和连字符命名，例如 `public/images/terminal-layout.png`。
3. 在文章中写入图片及其说明：

```md
![终端工作流的示意图](../images/terminal-layout.png)
```

文章的地址位于 `/posts/...` 下，所以站内图片要以 `../images/` 开头。图片说明不要省略：它会在图片无法加载时出现，也能帮助读屏软件使用者理解内容。

建议优先使用压缩后的 WebP、JPEG 或 PNG。截图要裁掉无关窗口和敏感信息；一张图片只服务一个明确观点。也可以使用完整的 `https://` 图片地址，但外部图片失效后会直接影响文章，因此重要图片最好保存在本站。

## 本地预览与发布前检查

在项目根目录运行：

```bash
npm run dev
```

打开终端显示的本地地址。先在首页确认新文章是否排在预期位置，再打开 `/posts/你的文件名` 检查标题、图片、代码和公式。开发服务会在保存 Markdown 后自动刷新。

准备发布前，再运行：

```bash
npm run build
```

构建通过说明全部页面可以生成。随后将新文章和图片提交并推送到 `main` 分支；项目的 GitHub Actions 工作流会自动发布站点。首次设置、发布检查和故障排查见 [GitHub Pages 部署教程](./DEPLOYMENT.md)。

## 发布前的最后一分钟清单

- [ ] 文件在 `content/posts` 下，扩展名为 `.md`。
- [ ] 文件名简短、唯一，并且发布后不会随意改动。
- [ ] frontmatter 位于文件最顶部，两条 `---` 都存在。
- [ ] `title`、`excerpt`、`date`、`readingTime` 和至少一个 `tags` 已填写。
- [ ] `date` 使用 `YYYY-MM-DD`，并确认它不会让文章排到错误的位置。
- [ ] 标题从 `##` 开始，层级没有跳跃。
- [ ] 所有链接、图片路径和代码围栏都已检查。
- [ ] 图片有准确的文字说明，且不含不应公开的信息。
- [ ] 在手机宽度和深色模式下快速通读一次。
- [ ] `npm run build` 已通过。

## 常见问题

### 新文章没有出现在首页

确认文件位于 `content/posts` 的直接目录中，而不是其子目录；文件名必须以 `.md` 结尾。再确认 frontmatter 的开头和结尾各有一行单独的 `---`，以及日期格式正确。

### 首页顺序不对

首页按 `date` 的文字顺序倒排。使用固定的 `YYYY-MM-DD` 格式，并检查日期是否写成了未来日期。

### 图片显示不出来

检查图片实际位置是否为 `public/images/文件名`，并确认 Markdown 中的路径是 `../images/文件名`。文件名大小写必须完全一致。

### 公式或代码没有正常显示

公式要成对使用 `$` 或 `$$`；代码块要成对使用三个反引号。最常见的问题是少写了结尾符号，或把普通美元符号误当成公式分隔符。

### 改完文章后旧链接失效

文章 URL 来自文件名。若文章已被分享，尽量只改内容而不改文件名。当前博客没有自动重定向；需要换链接时，建议保留旧文件并在正文顶部说明新文章地址。

---

第一次写作时，最省心的方法就是复制模板、先写一个清晰的开场和两个 `##` 小节、预览后再补图片和细节。内容先成形，排版自然会跟上。
